<% if user_signed_in? && user == current_user %>

<% end %>
  
  <h3>User Info</h3>
      <table class="table">
        <tr>
        <%= attachment_image_tag user, :profile_image_id, :fill, 50, 50, fallback: "no_image.jpg"%>
        </tr>
        <tr>
        </br>
        </br>
        <td><div class="fontwhite">UserName：</div></td>
        <td><div class="fontwhite"><%= user.family_name %><%= user.first_name %></div></td>
        </tr>
        <tr>
        <td><div class="fontwhite">Follows：</div></td>
        <td>
          <%= link_to following_user_path(user) do %>
            <%= user.followings.count %>
          <% end %>
        </td>
        </tr>
        <tr>
        <td><div class="fontwhite">Followers：</div></td>
        <td>
          <%= link_to followers_user_path(user) do %>
            <%= user.followers.count %>
          <% end %>
        </td>
        </tr>
        <td><%= render 'relationships/follow_button',user: user, post: post %></td>
      </table>
      
      <% if user_signed_in? && user == current_user %>
      <%= link_to  my_page_edit_path(user) do %>
      <i class="fas fa-user-cog fa-2x"></i>
      <% end %>
      <% end %>

      
      </br>